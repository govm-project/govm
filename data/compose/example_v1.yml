---
networks:
  - name: test
    subnet: 192.168.200.0/24

vms:
  - name: vmOne
    image: ~/vms/images/xenial-server-cloudimg-amd64-disk1.img
    cloud: true
    sshkey: ~/.ssh/id_rsa.pub
    user-data: |
      #!/bin/bash
      apt-get install emacs-nox -y
    network:
      net-id: test
      ip: 192.168.200.10

  - name: vmTwo
    image: ~/vms/images/xenial-server-cloudimg-amd64-disk1.img
    cloud: true
    sshkey: ~/.ssh/id_rsa.pub
    user-data: |
      #!/bin/bash
      apt-get install emacs-nox -y
    network:
      net-id: test
      ip: 192.168.200.20
